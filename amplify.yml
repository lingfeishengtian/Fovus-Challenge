version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    artifacts:
      baseDirectory: build
      files:
        - '**/*'
    build:
      commands:
        - npm run build